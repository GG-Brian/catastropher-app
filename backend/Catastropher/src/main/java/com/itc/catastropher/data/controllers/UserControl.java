/*package com.itc.catastropher.data.controllers;

import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import com.itc.catastropher.data.model.User;
//import com.itc.catastropher.data.service.IUserService;

@CrossOrigin(origins = "http://localhost:8100")
@RestController
public class UserControl {

	//@Autowired IUserService userService;
	
	/*@GetMapping("/user")
	User getOne(String name, String Password) {
		Optional<User> theUser = userService.getOne(name, Password);
		
		if(theUser.isPresent()) {
			return theUser.get();
		}
		
		return null;
	}
	
}*/